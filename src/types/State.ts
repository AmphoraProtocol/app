export type ThemeName = 'system-prefs' | 'light' | 'dark';

export interface ThemeState {
  current: ThemeName;
}

export interface RootState {
  theme: ThemeState;
}

export interface RootState {
  theme: ThemeState;
}
